{
  "name": "slides",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "slides": "npx marp -sw --theme ./theme.css ./slides",
    "dev": "echo \"Error: Please use $ npm run slides\" && exit 1",
    "pdf": "marp --pdf --input-dir ./slides --output ./pdf --theme ./theme.css",
    "build": "marp --input-dir ./slides --output ./build --theme ./theme.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@marp-team/marp-cli": "^1.4.0"
  }
}
